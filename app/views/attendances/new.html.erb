<% flash.each do |name, msg| -%>
          <%= content_tag :div, msg, class: name %>
    <% end -%>
  <h1> Pick a seat </h1>
  <%= render 'form' %> <!--loads in the page the form templated defined in _form.html.erb-->   
    <%= link_to 'Back', students_path %>
